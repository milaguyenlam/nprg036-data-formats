Query-1-1.jq deletes the last 2 elements inside the Rating array, which lies in the first object of Product array.

Query-1-2.jq lists all attributes.

Query-1-3.jq constructs objects consisting of the product description and price.

Query-1-4.jq extracts all JSON paths, except array element indices.